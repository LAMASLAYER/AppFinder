DROP TABLE IF EXISTS ASSETS;
CREATE TABLE ASSETS
(
  ASSET_ID INT NOT NULL UNIQUE AUTO_INCREMENT,
  URL VARCHAR(255) NOT NULL,
  CATEGORY VARCHAR(64),
  PRIMARY KEY (ASSET_ID)
);


INSERT INTO ASSETS
VALUES (2, 'this is an url', 'CATEGORYTEST');
